package com.jobpilot.resume.repository;

import com.jobpilot.resume.domain.ResumeVersionLanguage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ResumeVersionLanguageRepository extends JpaRepository<ResumeVersionLanguage, Long> {
}
